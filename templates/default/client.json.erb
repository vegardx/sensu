{
  "client": {
    "name": "<%= node[:hostname] %>",
    "address": "<%= node[:ipaddress] %>",
    "subscriptions": [
      "base"
    ]
  },
  "checks": {
    "cron_check": {
      "command": "/etc/sensu/plugins/check-procs.rb -p crond -C 1 ",
      "interval": 10,
      "standalone": true
    }
  }
}
